<a href="/">Home</a>
    <h1>Artist albums:</h1>
    {{#each items}}
        <h2>name: {{ name }}</h2>

            <img src="{{images.1.url}}" alt="{{name}}">

        <a href="/tracks/{{id}}">View Tracks</a>
    {{/each}}